/**
 * AppLayout component — not used in Phase 1.
 *
 * The application layout is handled by RootLayout (AppShell) and
 * ClubLayout (club tabs). This file is kept for potential future use.
 *
 * See 04_FE_SPEC.md section 9 (Frontend Architecture).
 */

// Intentionally empty — layout is handled by RootLayout and ClubLayout
export {}
