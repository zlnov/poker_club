/**
 * Loading state component.
 *
 * Re-exports from the UI primitives directory.
 *
 * See 04_FE_SPEC.md section 23 (Loading States) and
 * 05_FE_UX.md section 11 (Loading / Empty / Error States).
 */

export { LoadingState, InlineLoading } from './ui/LoadingState'
export type { LoadingStateProps } from './ui/LoadingState'
